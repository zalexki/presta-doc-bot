
CRON every day at 03AM -> call getNewCommitsCommand

- Entity
-- MergeCommit (id, createdAt, updatedAt, committedAt, idMergeCommit, authorName, commitMessage, idPullRequest, categories)
-- Category

- Services
-- Factory
--- GuzzleFactory
--- GithubGuzzleFactory 

-- Importer
--- MergeCommitImporter (transform a mergecommit from github to our entity mergecommit)

-- Command
--- getNewCommitsCommand (list commits from github api and add the new ones to the DB )

-- Controller
--- Homepage -> view list last merge commit (last 50...), pagination or load more ...
